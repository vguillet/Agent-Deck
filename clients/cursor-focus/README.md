# Agent Deck Focus for Cursor

This private companion extension handles `cursor://agent-deck.focus/open`
links from Agent Deck and opens the matching local Cursor conversation. It
does not read or transmit conversation content.

Install and manage it with:

```sh
agent-deck cursor-focus install
agent-deck cursor-focus status
agent-deck cursor-focus uninstall
```
