{
  "server": {
    "host": "127.0.0.1",
    "port": 47831
  },
  "retentionDays": 30,
  "staleAfterMs": 300000,
  "healthIntervalMs": 30000,
  "providers": [
    {
      "id": "codex",
      "module": "@agent-deck/provider-codex",
      "enabled": true,
      "discoveryIntervalMs": 15000,
      "config": {
        "binary": "codex"
      }
    },
    {
      "id": "cursor-cloud",
      "module": "@agent-deck/provider-cursor-cloud",
      "enabled": true,
      "discoveryIntervalMs": 30000,
      "config": {
        "apiKeyEnv": "CURSOR_API_KEY",
        "includeArchived": false
      }
    },
    {
      "id": "cursor-local",
      "module": "@agent-deck/provider-cursor-local",
      "enabled": true,
      "discoveryIntervalMs": 15000,
      "config": {}
    }
  ]
}
